
public class DifferenStatesOfThread {

//	new state- the time creation it is new thread it will be in new State
//	Runnable- when we say Start() it goes into Runnable state. ur thread is executing and it is waiting for the scheduler it is Runnable state
//	Waiting-  to keep the thread on hold by using sleep() method it will be in waiting stage apart from sleep() there will another method called wait() when u want to come back u should use the method notify() u wont come back to running state u will be in runnable state
//	Running- when ur thread is actually running in the cpu it is said to running thread - when the run() method then it is in running stage
//	Dead- stop()- to stop the running thread
	
}
